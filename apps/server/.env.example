DATABASE_URL="postgresql://enotebook:enotebook_secure_pwd@localhost:5432/enotebook?schema=public"
PORT=4000
NODE_ENV=development
CORS_ORIGINS="http://localhost:5173,http://127.0.0.1:5173"

# Local bootstrap admin account (seeded only when DB has zero users)
SEED_DEFAULT_ADMIN=true
DEFAULT_ADMIN_USERNAME=Admin
DEFAULT_ADMIN_PASSWORD=D_Admin
DEFAULT_ADMIN_EMAIL=admin@local

# Optional hardening
# AUTH_JWT_SECRET=replace-with-random-32+char-string
# BOOTSTRAP_ADMIN_SECRET=replace-with-random-bootstrap-secret
